{
  "name": "loom-hooks",
  "version": "1.0.0",
  "description": "Loom orchestration hooks for Claude Code",
  "type": "module",
  "scripts": {
    "test": "npm run test:unit && env -u PI_CODING_AGENT npm run test:smoke",
    "test:unit": "env -u PI_CODING_AGENT vitest run --testTimeout=15000",
    "test:watch": "env -u PI_CODING_AGENT vitest",
    "test:smoke": "bash ../scripts/smoke-panel-mode.sh && bash ../scripts/smoke-review-panel.sh && bash ../scripts/smoke-standalone-review.sh && bun ../scripts/smoke-orchestration-facades.ts && bash ../scripts/smoke-pi-resources.sh && bash ../artifacts/tests/test-validate-task-graph.sh",
    "calibrate:models": "LOOM_RUN_MODEL_CALIBRATION=1 bun ../scripts/run-model-calibration.ts",
    "typecheck": "bunx tsc --noEmit && npm run typecheck:unused",
    "typecheck:unused": "bunx tsc --noEmit --noUnusedLocals --noUnusedParameters 2>&1 | grep -E '^(\\.\\./pi/|(src|tests)/)' && exit 1 || exit 0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "fast-check": "^4.5.3",
    "ts-pattern": "^5.9.0",
    "typescript": "^5.0.0",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "@fuguejs/framework": "0.4.0",
    "zod": "4.3.6"
  }
}
