# Standalone review remediation — round 6

- Branch: `fix/abandoned-gate-terminal-outcome`; reviewed HEAD: `a6c1b244`.
- Exact frozen review scope: the 170 ordered paths in the registered `review-fix-20260924T075458Z-a6c1b244/result.json` (`scope`, digest `fae28948ebf2421ef4106f5dc5a3b7952b714e2ba64b044da40bb46b8414b3e9`). Do not infer a new scope from this plan.
- Published policy: `policy-20260924T081221Z-r6`, initial DECLARED revision, disposition digest `7bdcb9162ed252e99be63a00afdd18617336fb5354813d10e7264d4ef86e5f19`.
- Review inspection: 1 emitted/admitted critical, 8 advisories; the Refutation Panel upheld the critical under reproduction, intent and blast-radius; 0 refuted Findings.

## Surviving critical / repair group

- `code-reviewer-1`: **repaired**, group `group.merge-base-observation-failure-refusal`. Git merge-base `spawnSync` errors and fatal non-zero exits previously took the same `continue` arm as a clean no-common-ancestor result, allowing a null base to omit all committed branch changes from the frozen review scope.
- DECLARED root cause: a failed `observeGitProbe` observation was interpreted as candidate-specific absence regardless of whether it was a process error, Git fatal exit or ordinary no-base exit.
- DECLARED invariant: first verify each candidate ref with an independently observed `rev-parse --verify --quiet`; only a clean, observed absent ref or a clean merge-base exit-1 with empty stdout/stderr may skip a candidate. A spawn error, unexpected status, or Git diagnostic at either probe must refuse scope derivation with attribution. A status-0 empty revision still exhausts the existing bounded retry and refuses. A later valid candidate preserves the committed path family. Missing refs commonly give merge-base exit 128, so a blanket non-zero refusal without separate ref observation is also incorrect.
- DECLARED sibling accounting: `none-declared`. Other scope probes in this module (`gitText`, `gitPaths`, tracked and untracked numstat) already throw on failed observations; their *confirmed-empty* policies are separately explicit. The earlier report-reset guard is separately repaired; the advisory duplicate `silent-failure-hunter-1` is not a critical sibling.
- Selected operator-owned check: `project:verify`, required ignored/untracked JUnit report `.loom/completion-reports/verify.junit.xml` freshly generated by the command.
- Historical RED **DECLARED**: before production repair, the scripted merge-base spawn-error, fatal-exit and diagnostic-exit-1 assertions in `engine/tests/handlers/helpers/programs/reviewer-scope-empty-retry.test.ts` failed (see `/tmp/loom-r6-red.log`); the baseline focused suite passed 16/16 before tests were added. The repaired targeted suite passes 23/23, and the real temporary-repository successor lifecycle integration tests pass. The tests cover both a clean no-base fallback and an independently observed missing ref before trying a later valid candidate. These declarations are not engine-observed proof of historical behavior.

## Complete advisory policy (DECLARED)

| Finding | Decision | Reason / action |
| --- | --- | --- |
| `silent-failure-hunter-1` | Accepted | Duplicate symptom of the mandatory merge-base critical; distinguish clean no-base from unavailable Git authority in the same repair. |
| `type-design-analyzer-1` | Deferred | Persisted Wave live/retired authority needs a separate migration and consumers; no unsafe null use found in this review. |
| `type-design-analyzer-2` | Accepted | Require a confirmed-empty policy argument for every `gitPaths` call; current path-listing callers choose legitimate-empty explicitly. |
| `comment-analyzer-1` | Accepted | Correct the inaccurate universal-refusal comment on `confirmedEmptyPassthrough`; the caller owns empty policy. |
| `architecture-tech-lead-1` | Deferred | A new scope Git port would broaden this targeted repair and replace real-process authority coverage without solving the found failure more directly. |
| `architecture-tech-lead-2` | Deferred | Report reset is a separate destructive seam covered with no-follow and real-Git tests; a new port/fake has no finding-driven safety benefit here. |
| `code-simplifier-1` | Accepted | Replace the redundant string/Buffer branch with the same UTF-8 `toString()` and empty fallback. |
| `code-simplifier-2` | Accepted | Extract repeated stale-report fixture setup without altering its Git and filesystem effects or assertions. |

## Validation and install

Run the targeted Vitest suite, `cd engine && npm run verify` (typecheck, fresh structured Vitest report, smoke checks), and repository checks. Start a fresh schema-v2 remediation with this plan as the sole out-of-scope support path, account for the original Finding ID without replacing it with the group ID, and accept only a fresh engine-observed `repair-checked` result and exact verified-index installation. Commit that installed index without restaging, then push without force.
